package com.gwent.gwentapplication.deck;

import java.io.Serializable;
import java.util.Objects;

/*public class GwentDeckKey  implements Serializable {
        private Long userID;
        private Long cardId;

        // Default-Konstruktor
        public GwentDeckKey() {}

        public GwentDeckKey(Long userID, Long cardId) {
            this.userID = userID;
            this.cardId = cardId;
        }

        // equals & hashCode (wichtig für JPA)
        @Override
        public boolean equals(Object o) {
            if (this == o) return true;
            if (!(o instanceof GwentDeckKey)) return false;
            GwentDeckKey that = (GwentDeckKey) o;
            return Objects.equals(userID, that.userID) && Objects.equals(cardId, that.cardId);
        }

        @Override
        public int hashCode() {
            return Objects.hash(userID, cardId);
        }

}*/
